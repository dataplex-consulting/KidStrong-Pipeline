version: 2

models:
  - name: example_model
    description: "An example model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null